# These bundled reference files have byte-level SHA-256 sidecars. Keep their
# checked-out representation identical on every supported platform.
/src/metallocofactors/metallocofactors_id.txt text eol=lf
/src/coordination/metal_distances/metal_distances_info.txt text eol=lf
# Preserve the bundled reference byte for byte, including CSV CRLF.
/src/confidence_score/confidence_reference/component_distributions.csv -text
/src/confidence_score/confidence_reference/metadata.json -text
