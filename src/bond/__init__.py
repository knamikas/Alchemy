"""Metal-donor coordination: what contacts a metal has, and how good they are.

``bond_analysis.run_bond_analysis`` is the entry point the worker calls, and
``bond_schema`` holds the published CSV columns the driver reads. Nothing in
here knows about density, entries or the pool.
"""
